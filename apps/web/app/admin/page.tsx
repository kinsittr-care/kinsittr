import ScreeningQueueView from "@/src/components/admin/ScreeningQueueView";

export const metadata = { title: "Screening Queue — KinSittr Admin" };

export default function AdminPage() {
  return <ScreeningQueueView />;
}
